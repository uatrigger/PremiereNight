import { Dimensions } from 'react-native';

export const SCREEN_WIDTH = Dimensions.get('window').width;

export const CARD_INTERVAL = 16;
export const CARD_WIDTH = SCREEN_WIDTH * 0.85;
export const CARD_HEIGHT = SCREEN_WIDTH * 0.7;
